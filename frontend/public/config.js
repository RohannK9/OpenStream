// Runtime config (overridden in Docker container at startup).
window.__OPENSTREAM_CONFIG__ = {
  apiBaseUrl: "http://localhost:8000",
  grafanaUrl: "http://localhost:3001",
  prometheusUrl: "http://localhost:9090"
};

